faster-whisper>=1.0.0   # 本地语音识别（Whisper）
sounddevice>=0.4.6      # 麦克风录音
numpy>=1.26             # 音频处理
pypinyin>=0.50          # 唤醒词拼音模糊匹配
pillow>=10.0            # 桌宠 HUD 绘制
mcp>=1.0                # MCP 工具协议（可选功能）
websocket-client>=1.7   # 讯飞云端语音识别（可选；不接讯飞可不装）
# 大模型通过中转站的 OpenAI 兼容接口调用，用标准库 urllib，无需额外 SDK。
